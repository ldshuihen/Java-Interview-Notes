# 最终补全｜**面试冷门+高阶+金融/电商硬核场景**4

## 46. 多级消息队列削峰设计

- 流量突刺：普通队列 + 积压缓冲队列
- 高峰流量先进缓冲队列，低峰慢慢消费
- 业务隔离：核心业务、非核心业务拆分不同MQ集群
- 避免大流量拖垮核心链路

## 47. 购物退款/逆向流程设计

- 正向订单、逆向退款单双向状态机
- 退款幂等：退款单号唯一约束
- 资金原路退回、库存逆向归还
- 部分退款、全额退款、超时自动拒绝



## **购物退款 / 逆向流程系统设计**-GPT

 是电商系统中非常重要的一部分，涉及 **订单系统、支付系统、库存系统、资金系统**。大型电商如：

- Alibaba Group 的 Taobao
- JD
- Amazon

都存在复杂的 **正向交易 + 逆向退款体系**。

下面给你一套 **架构面试级退款系统设计**。

------

### 一、退款系统核心目标

退款系统解决的问题：

```
用户付款成功
↓
申请退款
↓
资金退回
↓
库存恢复
```

核心要求：

- 资金安全
- 状态一致
- 支持部分退款
- 幂等处理

------

### 二、正向订单 vs 逆向退款

电商系统通常有 **两个状态机**：

```
正向订单
逆向退款
```

#### 正向订单状态机

```
INIT
↓
PAID
↓
SHIPPED
↓
DELIVERED
↓
FINISHED
```

异常状态：

```
CANCELLED
REFUNDED
```

------

#### 逆向退款状态机

```
INIT
↓
REFUND_APPLY
↓
REFUND_APPROVED
↓
REFUND_PROCESSING
↓
REFUND_SUCCESS
```

失败：

```
REFUND_FAILED
```

------

### 三、退款系统整体架构

典型架构：

```
          用户申请退款
                │
                ▼
          Refund Service
                │
      ┌─────────┼─────────┐
      ▼         ▼         ▼
  Order Service Payment Service Inventory Service
      │
      ▼
   Refund DB
```

职责：

| 服务              | 职责     |
| ----------------- | -------- |
| Order Service     | 校验订单 |
| Payment Service   | 执行退款 |
| Inventory Service | 归还库存 |

------

### 四、退款单设计

退款系统必须有 **独立退款单**。

表：

```
refund_order
```

字段：

```
refund_id
order_id
user_id
refund_amount
refund_status
refund_reason
create_time
update_time
```

------

### 五、退款幂等设计（面试重点）

退款接口可能重复调用：

例如：

- 支付平台重复回调
- 网络重试

解决方案：

#### 1 退款单号唯一

```
refund_no UNIQUE
```

重复请求：

```
insert refund
```

触发：

```
DuplicateKey
```

说明已经创建。

------

#### 2 状态机控制

退款状态：

```
REFUND_PROCESSING
```

如果再次请求：

```
直接返回当前状态
```

------

#### 3 支付退款幂等

支付系统：

```
refund_request_id
```

唯一。

------

### 六、资金原路退回

退款必须：

```
原路退回
```

例如：

用户支付：

- Alipay
- WeChat

退款流程：

```
Refund Service
↓
Payment Service
↓
第三方支付
↓
回调通知
```

支付平台回调：

```
refund_success
```

------

### 七、库存逆向归还

退款成功后：

```
库存恢复
```

流程：

```
退款成功
↓
发送MQ
↓
Inventory Service
↓
库存增加
```

MQ示例：

```
refund_success_topic
```

优点：

```
解耦
异步处理
```

------

### 八、部分退款设计

订单可能：

```
3件商品
```

用户退：

```
1件
```

数据库：

```
refund_item
```

字段：

```
refund_item_id
refund_id
sku_id
refund_quantity
refund_amount
```

计算：

```
退款金额 = 单价 × 数量
```

------

### 九、全额退款设计

全额退款条件：

```
未发货
```

流程：

```
取消订单
↓
全额退款
↓
库存恢复
```

订单状态：

```
REFUNDED
```

------

### 十、超时自动拒绝

退款可能需要：

```
商家审核
```

设计：

```
退款申请
↓
等待审核
↓
超时自动拒绝
```

实现方式：

#### 1 延迟消息

使用：

```
RocketMQ 延迟消息
```

------

#### 2 定时任务

例如：

```
xxl-job
```

扫描：

```
pending refund
```

------

### 十一、退款一致性设计

退款涉及多个系统：

```
订单
支付
库存
```

需要保证一致性。

解决方案：

------

#### 1 事务消息

流程：

```
退款成功
↓
发送MQ
↓
库存恢复
```

使用：

```
RocketMQ事务消息
```

------

#### 2 Saga模式

分布式事务：

```
退款
库存补偿
```

------

### 十二、退款系统高并发设计

高并发场景：

```
双十一
```

优化：

------

#### 1 MQ削峰

退款请求：

```
进入MQ
```

------

#### 2 异步退款

退款流程：

```
创建退款单
↓
异步执行退款
```

------

#### 3 分库分表

退款单表：

```
按 order_id 分片
```

------

### 十三、退款系统完整流程

完整流程：

```
用户申请退款
       │
       ▼
 Refund Service
       │
       ▼
 校验订单状态
       │
       ▼
 创建退款单
       │
       ▼
 调用支付退款
       │
       ▼
 支付平台回调
       │
       ▼
 更新退款状态
       │
       ▼
 MQ通知库存恢复
       │
       ▼
 订单状态更新
```

------

### 十四、退款系统面试标准总结

标准回答可以这样说：

```
电商系统采用正向订单与逆向退款双状态机设计
退款系统通过退款单号保证幂等
资金通过支付系统原路退回
退款成功后通过MQ通知库存系统归还库存
支持部分退款与全额退款
通过延迟消息实现退款审核超时自动处理
```

------

如果你在准备 **电商 / Java架构 / 系统设计面试**，还有 **三个电商系统面试必问问题**：

1️⃣ **库存扣减系统设计（防止超卖）**
2️⃣ **优惠券系统设计（防刷 / 并发）**
3️⃣ **订单系统设计（分布式事务）**

我可以帮你整理一份 **《电商系统 25 个核心架构设计题（含完整架构图）》**，基本覆盖 **阿里 / 京东 / 拼多多 系统设计面试题库**。

## 48. 抽奖/活动概率系统

- 概率权重配置、奖品库存限制
- 防薅羊毛：设备+IP+账号多维限制
- 中奖记录幂等、防止重复中奖
- 库存缓存预热、活动限流

## **抽奖 / 活动概率系统** -gpt

**抽奖 / 活动概率系统** 是互联网活动平台（营销活动、用户增长）的常见系统设计题。很多平台都会使用，例如：

- Alibaba Group 的 Taobao 活动抽奖
- Tencent 的 WeChat 小程序活动
- ByteDance 的 Douyin 活动运营

该系统重点考察 **概率算法、库存控制、防刷策略、幂等设计、高并发处理**。

下面是一套 **面试级抽奖系统设计**。

------

### 一、抽奖系统核心需求

用户参与活动：

```
点击抽奖
↓
系统计算中奖结果
↓
返回奖品
↓
记录中奖
```

非功能需求：

```
高并发
公平概率
库存控制
防作弊
```

例如：

```
活动同时10万QPS
```

------

### 二、系统整体架构

典型架构：

```
           用户
            │
            ▼
       API Gateway
            │
            ▼
       Lottery Service
            │
     ┌──────┴────────┐
     ▼               ▼
   Redis           MySQL
 (库存/缓存)      (中奖记录)
     │
     ▼
 Message Queue
     │
     ▼
 奖品发放服务
```

模块职责：

| 模块            | 职责     |
| --------------- | -------- |
| Lottery Service | 抽奖逻辑 |
| Redis           | 奖品库存 |
| MySQL           | 中奖记录 |
| MQ              | 异步发奖 |

------

### 三、概率权重配置

抽奖奖品需要配置概率。

表：

```
lottery_prize
```

字段：

```
prize_id
activity_id
prize_name
probability
stock
```

示例：

| 奖品     | 概率  |
| -------- | ----- |
| 一等奖   | 0.1%  |
| 二等奖   | 1%    |
| 三等奖   | 10%   |
| 谢谢参与 | 88.9% |

------

### 四、抽奖算法设计

常见算法：

#### 1 权重随机算法

计算：

```
总权重 = 10000
```

示例：

```
一等奖 = 10
二等奖 = 100
三等奖 = 1000
未中奖 = 8890
```

生成随机数：

```
rand(1~10000)
```

区间判断中奖。

------

#### 2 Alias算法（高性能）

当奖品很多时：

```
O(1) 抽样
```

适合：

```
高并发抽奖
```

------

### 五、奖品库存控制

抽奖必须控制库存。

库存放在：

```
Redis
```

Key：

```
lottery:stock:prizeId
```

扣减：

```
DECR
```

流程：

```
抽中奖品
↓
Redis库存扣减
↓
库存 >0 才成功
```

避免：

```
超发奖品
```

------

### 六、库存缓存预热

活动开始前：

```
DB库存
↓
加载到Redis
```

例如：

```
SET lottery:stock:1001 500
```

优点：

```
减少数据库压力
```

------

### 七、防薅羊毛设计（重点）

抽奖活动非常容易被刷。

常见作弊：

```
多账号
刷接口
脚本攻击
```

解决方案：

------

#### 1 用户限制

限制：

```
每人每天1次
```

Redis：

```
SETNX user:lottery:uid
```

------

#### 2 设备限制

记录：

```
device_id
```

限制：

```
同设备抽奖次数
```

------

#### 3 IP限制

例如：

```
IP 每分钟10次
```

使用：

```
Redis + 限流
```

------

#### 4 行为风控

风控系统分析：

```
IP
设备
用户
```

评分：

```
risk_score
```

高风险用户：

```
拒绝抽奖
```

------

### 八、中奖记录幂等

防止重复中奖。

中奖记录表：

```
lottery_record
```

字段：

```
record_id
user_id
activity_id
prize_id
status
create_time
```

唯一约束：

```
(user_id, activity_id, prize_id)
```

保证：

```
不会重复中奖
```

------

### 九、中奖异步发放

中奖后通常：

```
异步发奖
```

流程：

```
抽中奖品
↓
写中奖记录
↓
发送MQ
↓
奖品发放服务
```

奖品类型：

```
优惠券
积分
实物奖品
```

MQ示例：

```
Kafka
RocketMQ
```

------

### 十、活动限流设计

活动开始时：

```
流量暴增
```

解决：

------

#### 1 网关限流

例如：

```
Nginx
```

------

#### 2 服务限流

例如：

```
Sentinel
```

------

#### 3 队列削峰

抽奖请求：

```
进入MQ
```

再消费。

------

### 十一、抽奖系统高并发优化

高并发优化策略：

------

#### 1 Redis全缓存

抽奖数据：

```
概率
库存
```

全部缓存。

------

#### 2 无锁抽奖

避免：

```
数据库锁
```

------

#### 3 分布式锁

库存更新：

```
Redisson
```

------

#### 4 热点Key优化

热门奖品：

```
分桶Key
```

------

### 十二、抽奖系统完整流程

完整流程：

```
用户点击抽奖
       │
       ▼
 Lottery Service
       │
       ▼
 校验用户资格
       │
       ▼
 权重随机算法
       │
       ▼
 Redis库存扣减
       │
       ▼
 写中奖记录
       │
       ▼
 MQ发奖
```

------

### 十三、面试标准总结

面试回答可以这样说：

```
抽奖系统通过权重概率算法计算中奖结果
奖品库存缓存到Redis避免数据库压力
通过Redis原子扣减控制库存
中奖记录通过唯一索引保证幂等
使用MQ异步发放奖品
通过用户、设备、IP多维度限制防止薅羊毛
活动高峰通过限流和MQ削峰保护系统
```

------

如果你在准备 **Java架构 / 系统设计面试**，还有 **三个活动系统高频设计题**，很多大厂会问：

1️⃣ **红包系统设计（微信红包）**
2️⃣ **优惠券系统设计（发券 / 防刷）**
3️⃣ **签到系统设计（连续签到奖励）**

我可以帮你整理一份 **《互联网活动系统 15 个架构设计题（含概率算法+架构图）》**，这类题在 **阿里 / 字节 / 腾讯 面试中非常常见**。

## 49. 地区/省市区三级数据设计

- 树形结构、编码层级（6位地区码）
- 全量数据本地缓存+Redis持久化
- 懒加载、树形递归查询
- 行政区域定时同步更新

## **地区 / 省市区三级数据设计**-GPT

 是电商、物流、用户地址管理中的经典系统设计问题。很多平台都会使用，例如：

- Alibaba Group 的 Taobao 地址选择
- JD.com 的电商配送地址系统
- Meituan 的外卖配送区域

核心难点在于：

```
数据结构设计
高效查询
缓存策略
数据同步更新
```

下面是 **面试级完整设计方案**。

------

### 一、行政区划数据特点

中国行政区数据结构：

```
省
 └─ 市
     └─ 区县
```

例如：

```
浙江省
 └─ 杭州市
     └─ 西湖区
```

特点：

```
稳定
层级固定
查询频繁
更新极少
```

因此设计原则：

```
读多写少
强缓存
树结构
```

------

### 二、数据库表设计

常见设计：**单表树结构**

表：

```
region
```

字段：

| 字段      | 说明     |
| --------- | -------- |
| id        | 行政编码 |
| name      | 地区名称 |
| parent_id | 父节点   |
| level     | 层级     |
| full_name | 全路径   |
| sort      | 排序     |

示例：

| id     | name   | parent_id | level |
| ------ | ------ | --------- | ----- |
| 110000 | 北京市 | 0         | 1     |
| 110100 | 北京市 | 110000    | 2     |
| 110101 | 东城区 | 110100    | 3     |

行政编码规则：

```
6位地区码
```

例如：

```
110000 省
110100 市
110101 区
```

------

### 三、树形结构设计

行政区天然是 **树结构**。

两种方式：

#### 1 邻接表（推荐）

```
parent_id
```

优点：

```
简单
更新方便
```

SQL查询：

```sql
select * from region where parent_id = ?
```

------

#### 2 路径编码

增加字段：

```
path
```

例如：

```
/110000/110100/110101
```

优点：

```
快速查询祖先节点
```

------

### 四、查询接口设计

常见接口：

#### 1 查询省份

```
GET /region/province
```

SQL：

```sql
select * from region where level=1
```

------

#### 2 查询城市

```
GET /region/city?provinceId=110000
```

SQL：

```sql
select * from region where parent_id=110000
```

------

#### 3 查询区县

```
GET /region/district?cityId=110100
```

SQL：

```sql
select * from region where parent_id=110100
```

------

### 五、树形递归查询

有时需要 **完整地区树**：

```
省
 └市
   └区
```

Java递归构建：

```java
List<Region> buildTree(Long parentId){
    List<Region> children = query(parentId);
    for(Region r : children){
        r.setChildren(buildTree(r.getId()));
    }
    return children;
}
```

返回：

```
JSON Tree
```

------

### 六、本地缓存设计（最佳实践）

行政区数据 **几乎不变**。

因此推荐：

```
启动加载 → 本地缓存
```

例如：

```
Map<Long, Region>
```

加载流程：

```
服务启动
↓
加载DB
↓
存入内存
```

查询：

```
直接内存查
```

优点：

```
极快
无数据库压力
```

------

### 七、Redis缓存设计

Redis缓存结构：

Key：

```
region:tree
```

Value：

```
完整JSON树
```

或者：

```
region:{parentId}
```

示例：

```
region:0
region:110000
```

缓存查询流程：

```
用户请求
↓
Redis
↓
DB
```

------

### 八、懒加载设计（前端常用）

前端地址选择通常：

```
三级联动
```

流程：

```
选择省
↓
加载市
↓
加载区
```

接口：

```
/region/list?parentId=110000
```

优点：

```
数据量小
加载快
```

------

### 九、行政区域更新机制

行政区数据来源：

```
国家统计局
民政部
```

更新方式：

#### 定时任务同步

例如：

```
每天检查
```

流程：

```
定时任务
↓
下载最新行政区
↓
比对差异
↓
更新DB
↓
刷新缓存
```

工具：

```
xxl-job
Quartz
```

------

### 十、缓存刷新策略

更新后需要：

```
刷新缓存
```

方案：

#### 1 发布订阅

```
Redis PubSub
```

通知所有服务刷新。

------

#### 2 版本号

缓存key：

```
region:tree:v2
```

版本变化自动刷新。

------

### 十一、高并发优化

行政区查询 QPS 可能非常高。

优化方案：

#### 1 本地缓存

```
Guava Cache
Caffeine
```

------

#### 2 Redis缓存

```
TTL = 24h
```

------

#### 3 启动预热

```
服务启动加载所有地区
```

------

### 十二、完整架构

系统架构：

```
           用户
            │
            ▼
        API Gateway
            │
            ▼
       Region Service
            │
     ┌──────┴──────┐
     ▼             ▼
  Local Cache     Redis
                     │
                     ▼
                    MySQL
```

查询优先级：

```
Local Cache
↓
Redis
↓
MySQL
```

------

### 十三、面试标准回答

面试回答可以这样总结：

```
行政区采用单表树结构设计，通过parent_id维护层级关系
地区编码采用6位行政区编码
系统启动时全量加载地区数据到本地缓存
同时缓存到Redis避免数据库压力
查询采用懒加载三级联动方式
行政区数据通过定时任务同步国家统计局数据
更新后通过缓存刷新机制同步所有节点
```

------

如果你是在准备 **Java架构 / 系统设计面试**，你刚问的这些其实属于 **互联网系统设计 100 题体系**。

接下来最经典的几个题是：

```
50 统一ID生成器（雪花算法）
51 分布式任务调度系统
52 全局搜索系统设计
53 推荐系统设计
54 排行榜系统设计
```

我可以给你整理一份 **《Java系统设计100题 + 标准架构答案（面试版）》**，基本覆盖 **阿里 / 字节 / 腾讯 架构面试题**。

## 50. 智能重试机制设计

- 接口超时、第三方抖动自动重试
- 指数退避策略：1s、2s、4s、8s
- 重试白名单、非幂等接口禁止重试
- 重试次数上限+熔断兜底

## 51. 分布式会话共享

- 集群登录态不互通问题
- 统一Redis存储Session
- 无状态JWT+网关校验
- 会话续期、单点下线、挤下线

## 52. 评论点赞/踩、热度排行

- 点赞状态Redis位图存储，极致省内存
- 计数Redis原子累加
- 定时批量落库，减少DB压力
- 防止刷赞：频率限制+行为风控

## **评论点赞 / 踩 + 热度排行系统** -GPT

是社区、电商、内容平台非常常见的系统设计。例如：

- Weibo 评论点赞
- Bilibili 评论热度排序
- Zhihu 评论赞踩机制

设计目标通常是：

```text
高并发点赞
低延迟查询
防刷赞
支持热度排行
```

下面是 **面试级系统设计方案**。

------

### 一、核心需求

用户行为：

```text
点赞评论
取消点赞
踩评论
取消踩
查看点赞数
评论按热度排序
```

非功能需求：

```text
高并发
数据准确
防作弊
低成本存储
```

规模示例：

```text
日点赞量：10亿+
评论数：1亿+
```

------

### 二、系统整体架构

典型架构：

```text
           用户
            │
            ▼
        API Gateway
            │
            ▼
        Like Service
            │
     ┌──────┴───────────┐
     ▼                  ▼
    Redis             MySQL
(点赞状态/计数)      (评论表)
     │
     ▼
 Message Queue
     │
     ▼
 批量落库服务
```

模块职责：

| 模块         | 作用            |
| ------------ | --------------- |
| Like Service | 点赞逻辑        |
| Redis        | 点赞状态 + 计数 |
| MySQL        | 评论数据        |
| MQ           | 异步持久化      |

------

### 三、数据库设计

评论表：

```sql
comment
```

字段：

| 字段          | 说明     |
| ------------- | -------- |
| comment_id    | 评论ID   |
| user_id       | 评论作者 |
| content       | 内容     |
| like_count    | 点赞数   |
| dislike_count | 踩数     |
| create_time   | 时间     |

点赞记录表（可选）：

```sql
comment_like
```

| 字段       | 说明       |
| ---------- | ---------- |
| user_id    | 用户       |
| comment_id | 评论       |
| status     | 1点赞 -1踩 |

但在高并发系统中：

```text
点赞状态主要放Redis
```

------

### 四、点赞状态存储（Redis位图）

为了节省内存，可以使用：

```text
Redis Bitmap
```

Key：

```text
comment:like:{commentId}
```

位图含义：

```text
offset = userId
1 = 已点赞
0 = 未点赞
```

示例：

```text
SETBIT comment:like:1001 12345 1
```

优点：

```text
极致省内存
查询极快
```

内存消耗：

```text
1亿用户 ≈ 12MB
```

------

### 五、点赞计数设计

点赞数量需要快速查询。

Redis计数：

Key：

```text
comment:like:count:{commentId}
```

点赞：

```text
INCR
```

取消点赞：

```text
DECR
```

Redis保证：

```text
原子操作
```

------

### 六、点赞流程

完整流程：

```text
用户点赞
     │
     ▼
判断是否已点赞
     │
     ▼
更新Redis位图
     │
     ▼
Redis计数 +1
     │
     ▼
发送MQ
```

MQ消息：

```text
like_event
```

------

### 七、批量落库设计

点赞量巨大，如果每次写DB：

```text
数据库会崩
```

解决方案：

```text
异步批量落库
```

流程：

```text
点赞
 ↓
MQ
 ↓
消费服务
 ↓
批量更新数据库
```

例如：

```sql
update comment
set like_count = like_count + ?
where comment_id = ?
```

批量提交：

```text
每1000条提交
```

------

### 八、热度排行设计

评论通常需要：

```text
按热度排序
```

热度算法：

```text
score = like - dislike
```

或者：

```text
score = like * 2 + reply + 浏览
```

------

### 九、Redis排行榜

使用：

```text
Redis ZSET
```

Key：

```text
comment:rank:{postId}
```

结构：

```text
score → commentId
```

更新：

```text
ZINCRBY
```

查询：

```text
ZREVRANGE
```

示例：

```text
ZREVRANGE comment:rank:1001 0 9
```

返回：

```text
Top10评论
```

------

### 十、防刷赞设计

点赞系统很容易被刷。

常见作弊：

```text
脚本点赞
批量账号
机器人
```

解决方案：

------

#### 1 频率限制

例如：

```text
1秒最多5次
```

Redis限流：

```text
INCR + TTL
```

Key：

```text
user:like:limit
```

------

#### 2 IP限制

例如：

```text
IP 每分钟100次
```

------

#### 3 设备限制

记录：

```text
deviceId
```

同设备限制点赞次数。

------

#### 4 行为风控

风控系统判断：

```text
异常点赞
异常账号
```

策略：

```text
降权
封禁
验证码
```

------

### 十一、热点评论优化

热门评论：

```text
点赞量巨大
```

优化：

#### 1 分片Key

```text
comment:like:count:{commentId}:{shard}
```

------

#### 2 合并统计

定期：

```text
聚合总数
```

------

### 十二、缓存一致性

Redis与DB一致性问题：

策略：

```text
最终一致性
```

流程：

```text
Redis实时更新
DB异步更新
```

定时任务：

```text
校对数据
```

------

### 十三、完整点赞流程

完整流程：

```text
用户点赞
     │
     ▼
Redis Bitmap 判断状态
     │
     ▼
更新Bitmap
     │
     ▼
Redis计数+1
     │
     ▼
ZSET更新热度
     │
     ▼
MQ发送事件
     │
     ▼
异步批量落库
```

------

### 十四、面试总结回答

面试可以这样回答：

```text
点赞系统通过Redis Bitmap记录用户点赞状态以节省内存
点赞计数通过Redis原子累加实现高并发更新
评论热度通过Redis ZSET维护排行榜
点赞事件通过MQ异步批量落库减少数据库压力
同时通过用户、IP、设备多维度限流和风控防止刷赞
```

------

如果你在准备 **Java系统设计面试**，接下来两个问题其实是 **大厂非常高频的题**：

```
53 排行榜系统设计（亿级数据）
54 推荐系统设计（猜你喜欢）
55 关注 / 粉丝系统设计
```

尤其是 **排行榜系统**，几乎所有内容平台都会问，我可以帮你整理一套 **从简单 → 亿级规模的完整设计方案**。

## 53. 内容审核系统

- 文本：敏感词字典树 + 第三方AI审核
- 图片：鉴黄、水印、违规识别
- 审核流转：待审核、审核通过、驳回
- 人工兜底、审核日志留痕

## 内容审核系统-GPT

**内容审核系统（Content Moderation System）** 是所有UGC平台的“安全中枢”，几乎所有内容型产品都会用到，例如：

- WeChat 朋友圈内容审核
- Douyin 视频/评论审核
- Bilibili 投稿与评论审核
- Zhihu 内容安全审核

核心目标：

```text
过滤违规内容 + 提高审核效率 + 保证结果一致性 + 可追溯
```

下面给你一套 **面试级内容审核系统设计（完整架构）**。

------

### 一、内容审核系统核心需求

用户上传内容：

```text
文本 / 图片 / 视频
```

系统需要：

```text
识别违规
决定是否通过
进入人工审核
记录审计日志
```

------

### 二、整体架构设计

典型架构：

```text
           用户上传内容
                  │
                  ▼
           API Gateway
                  │
                  ▼
           Content Service
                  │
        ┌─────────┼──────────┐
        ▼         ▼          ▼
   文本审核   图片审核   视频审核
        │         │          │
        ▼         ▼          ▼
  AI审核引擎   AI视觉模型   视频分析
        │
        ▼
   审核决策引擎
        │
   ┌────┴─────┐
   ▼          ▼
通过        人工审核队列
   │          │
   ▼          ▼
发布       审核后台
```

------

### 三、审核状态机设计（核心）

内容审核必须有状态流转：

```text
INIT（待提交）
 ↓
AUTO_PASS（机审通过）
 ↓
MANUAL_REVIEW（人工审核）
 ↓
APPROVED（通过）
 ↓
REJECTED（驳回）
```

扩展状态：

```text
BLOCKED（风控拦截）
DELETED（删除）
```

------

### 四、文本审核设计

#### 1 敏感词过滤（Trie树）

基础方案：

```text
Trie树 / DFA
```

流程：

```text
用户文本
 ↓
Trie匹配
 ↓
命中敏感词 → 拦截
```

优点：

```text
极快 O(n)
```

------

#### 2 升级：多层文本审核

组合策略：

```text
敏感词 + 语义模型 + AI审核
```

例如：

```text
暴力 / 色情 / 政治敏感
```

------

#### 3 第三方AI审核

例如：

- NLP模型
- 云内容安全API

流程：

```text
文本 → AI模型 → 风险评分
```

输出：

```text
risk_score = 0~100
```

------

### 五、图片审核设计

图片审核包含：

#### 1 鉴黄（NSFW检测）

```text
色情内容识别
```

------

#### 2 暴恐识别

```text
武器 / 暴力内容
```

------

#### 3 OCR识别违规文本

```text
图片中的文字检测
```

------

#### 4 水印检测

```text
防盗图
```

------

#### 图片审核流程

```text
图片上传
 ↓
对象存储
 ↓
异步任务（MQ）
 ↓
AI视觉模型
 ↓
返回审核结果
```

------

### 六、视频审核设计

视频审核更复杂：

流程：

```text
视频上传
 ↓
切帧
 ↓
逐帧图片审核
 ↓
音频转文字（ASR）
 ↓
文本审核
 ↓
综合判断
```

组件：

```text
FFmpeg + ASR + CV模型
```

------

### 七、审核流转设计（核心）

审核决策引擎：

```text
rule engine + AI score
```

规则：

```text
if score > 90 → 直接拒绝
if score 60~90 → 人工审核
if score < 60 → 通过
```

------

### 八、审核状态存储设计

表设计：

```sql
content_audit
```

字段：

| 字段         | 说明             |
| ------------ | ---------------- |
| content_id   | 内容ID           |
| content_type | text/image/video |
| status       | 审核状态         |
| risk_score   | 风险评分         |
| audit_result | 结果             |
| create_time  | 创建时间         |

------

### 九、审核队列设计（异步化）

使用：

```text
Kafka / RabbitMQ
```

流程：

```text
内容上传
 ↓
进入MQ
 ↓
审核服务消费
 ↓
AI审核
 ↓
更新状态
```

优点：

```text
削峰填谷
解耦系统
```

------

### 十、人工审核系统（兜底）

当AI无法判断：

```text
进入人工队列
```

审核后台：

```text
审核员系统
```

功能：

```text
通过 / 驳回 / 标记原因
```

------

### 十一、审核日志留痕（重点）

所有审核必须可追溯：

日志表：

```sql
audit_log
```

字段：

```text
content_id
operator
action
result
time
```

用途：

```text
合规审计
争议处理
模型训练
```

------

### 十二、高并发设计

审核系统特点：

```text
写入少，计算重
```

优化：

------

#### 1 异步审核

```text
上传即返回
后台审核
```

------

#### 2 多级缓存

```text
本地缓存 + Redis
```

------

#### 3 批量处理

```text
批量送AI模型
```

------

### 十三、防绕过设计（风控）

用户可能：

```text
拆分文本 / 图片变形 / 拼接
```

防御：

------

#### 1 用户行为分析

```text
点赞/发帖频率异常
```

------

#### 2 设备指纹

```text
deviceId + IP + token
```

------

#### 3 黑名单系统

```text
封禁用户
```

------

### 十四、完整系统流程

```text
用户上传内容
        │
        ▼
内容服务
        │
        ▼
写入DB + MQ
        │
        ▼
AI审核（文本/图像/视频）
        │
        ▼
决策引擎
        │
 ┌──────┴──────┐
 ▼             ▼
通过         人工审核
 │             │
 ▼             ▼
发布        审核后台
```

------

### 十五、面试标准总结（高分回答）

可以这样说：

```text
内容审核系统通过多层架构实现
文本使用Trie树敏感词过滤 + AI语义模型
图片通过视觉模型进行鉴黄和违规检测
视频通过抽帧 + OCR + ASR综合判断
审核流程采用状态机管理
通过MQ实现异步审核削峰
人工审核作为兜底机制
所有审核行为记录日志保证可追溯
```

------

### 十六、加分点（面试官很喜欢）

如果你再补一句：

```text
审核系统本质是“规则系统 + AI模型 + 风控系统”的组合
```

基本就是架构师水平。

------

如果你在准备系统设计面试，我可以帮你把这一整套串起来：

👉 **《互联网系统设计 60 题终极架构图》**

包含：

- 秒杀
- 订单
- 支付
- Feed流
- IM
- 推荐
- 审核
- 点赞
- 抽奖

基本覆盖 **P6-P8面试全范围**。

## 54. 异地容灾设计

- 主备机房、异地备份
- 数据实时/异步复制
- 故障自动切换、手动切换双方案
- 演练机制，定期容灾测试

## 55. 资源隔离设计

- 线程池隔离：核心/非核心业务分开线程池
- 集群隔离：大V、活动、普通用户拆分部署
- 中间件隔离：独立MQ、独立Redis
- 避免单一业务故障全局雪崩

## 56. 长连接海量连接管理

- WebSocket/Netty 百万连接
- 心跳保活、空闲连接回收
- 连接分片、分布式会话
- 断线重连、消息离线缓存

## 57. 版本兼容/接口兼容设计

- 向前兼容、字段新增不删除
- 多版本接口共存 /api/v1 /api/v2
- 枚举兼容、默认值兜底
- 灰度升级、平滑迭代

## 58. 库存锁定&释放设计

- 下单锁定库存、支付成功扣减
- 未支付超时自动释放
- 锁定记录落库+缓存双层校验
- 防止死锁库存、定时清理僵死数据

## 59. 数据迁移设计

- 双写双读、新旧库并行
- 全量迁移 + 增量同步
- 流量灰度切换、对比校验
- 一键回滚方案

## 60. 风控反作弊系统

- 用户行为采集：点击、停留、操作轨迹
- 规则引擎：批量风控规则
- 实时风控拦截+离线数据分析
- 羊毛党、脚本、爬虫识别拦截

## 61. 预付费/余额系统

- 余额原子扣减、流水账记录
- 冷热钱包分离
- 资金锁、冻结金额机制
- 日终对账、资金平账

## 62. 短时间高并发定时任务

- 整点大量任务同时爆发
- 任务分片、错峰执行
- 时间轮+延时队列削峰
- 集群分片执行，分散压力

## 63. 接口慢查询优化整体方案

- 慢SQL治理、索引优化
- 接口拆分、大接口解耦
- 同步改异步、缓存覆盖
- 限制返回字段、分页、按需加载

## 64. 数据备份与恢复

- 定时全量备份+增量binlog
- 跨机器备份、防止物理故障
- 恢复演练、时间点恢复
- 备份文件加密、过期清理

## 65. 多语言/国际化设计

- 多语言资源文件、配置中心统一管理
- 本地缓存+Redis缓存文案
- 请求头携带语言标识
- 默认语种兜底，防止展示异常

---

## ✅ 汇总完毕

**一共 65 个高频系统设计场景**
涵盖：
高并发、分布式、电商、金融、活动、缓存、MQ、网关、RPC、事务、锁、ID、限流、风控、容灾、迁移、长连接、SaaS、日志、监控、审核、推送、对账。

---

需要我给你：
**「65道系统设计 一页纸极简背诵版」**
每道题 2~3 句话，面试直接口述，极简好背？

#